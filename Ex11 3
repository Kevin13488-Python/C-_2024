#include <stdio.h>

int main()
{
    int myAge = 43;
    int *p;
    printf("myAge value = %d\n", myAge);
    printf("myAge's address in memory = %p\n", *myAge);
    p = &myage;
    printf("pointer p's value = %d\n", *p);
    return 0;
}
